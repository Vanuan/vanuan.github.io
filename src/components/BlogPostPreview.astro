---
const { title, description, publishDate, slug, series } = Astro.props;
---

<article class="bg-white dark:bg-gray-800 p-6 rounded-lg shadow-lg hover:shadow-xl transition-shadow">
  <h3 class="text-2xl font-bold mb-2">{title}</h3>
  <p class="text-gray-600 dark:text-gray-300 mb-4">{description}</p>
    {series && (
      <div class="mb-2">
        <span class="inline-block bg-blue-100 dark:bg-blue-900 text-blue-800 dark:text-blue-200 text-xs px-2 py-1 rounded">
          Series: {series}
        </span>
      </div>
    )}
  <p class="text-sm text-gray-500 dark:text-gray-400">
    Published on: {new Date(publishDate).toLocaleDateString()}
  </p>
  <a href={`/blog/${slug}`} class="mt-4 inline-block text-blue-500 hover:text-blue-700">
    Read more →
  </a>
</article>
